import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingVertical: 60,
    paddingHorizontal: 40,
  },
  title: {
    fontSize: 26,
    color: "#242424",
    textAlign: "center",
  },
});
