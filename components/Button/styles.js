import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    width: "auto",
    height: "auto",
    minWidth: 24,
    minHeight: 24,
    alignItems: "center",
    justifyContent: "center",
    paddingHorizontal: 2,
    borderRadius: 20000,
  },
});
