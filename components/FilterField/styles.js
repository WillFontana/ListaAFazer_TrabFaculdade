import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 40,
  },
  text: {
    marginBottom: 10,
  },
  field: {
    borderColor: "#CECFCE",
    borderWidth: 1,
    borderRadius: 6,
    paddingHorizontal: 5,
  },
});
